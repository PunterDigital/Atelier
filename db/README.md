# Database

Postgres via Drizzle ORM. Schema and checked-in migrations live here.
Every table that holds business data carries a `business_id` and every
query is scoped by it.

Not started yet - lands with the Drizzle/Postgres task in Phase 0.
